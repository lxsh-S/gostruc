# Internal

This folder contains:

```
blueprint/ --Contains files for project structure blueprints 
create/    --Contains files for generating the structure
define/    --Contains a create.go file that checks if right language name was passed
```

In simple terms it has both templates and files to that builds project structure
